{
    "name": "aj-ui-monorepo",
    "private": true,
    "version": "1.0.0",
    "workspaces": ["packages/*, apps/*"],
    "scripts": {
      "build": "pnpm -r run build",
      "dev": "pnpm -r run dev"
    }
}
