{
  "name": "flexi-ui-monorepo",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "packages/*, apps/*"
  ],
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm -r run dev",
    "release": "node scripts/release.js"
  },
  "devDependencies": {
    "@changesets/cli": "^2.28.1",
    "chalk": "^5.4.1",
    "prompts": "^2.4.2",
    "simple-git": "^3.27.0"
  }
}
